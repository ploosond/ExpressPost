<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
      <div class="w-full flex justify-end">
        <a
          class="bg-red-500 rounded-md px-3 py-2 mb-5 inline-block"
          href="/logout"
          >Logout</a
        >
      </div>

      <h5 class="mb-5">Edit your post.</h5>
      <form action="/update/<%= post._id%>" method="post">
        <textarea
          class="p-3 w-1/3 resize-none bg-transparent border-2 border-zinc-800 rounded-md"
          placeholder="What's on your mind?"
          name="content"
        >
<%= post.content %></textarea
        >
        <input
          class="px-3 py-2 w-40 rounded-md bg-yellow-500 text-black block mt-2 text-sm"
          type="submit"
          value="Update  Post"
        />
      </form>
    </div>
  </body>
</html>
